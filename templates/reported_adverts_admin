<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Reported Adverts</title>
    <style>
        /* Existing CSS from your provided reported_adverts_admin.html */
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            color: #333;
        }
        .container {
            max-width: 1200px;
            margin: 20px auto;
            background: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        .top-navigation {
            display: flex;
            justify-content: flex-start;
            gap: 15px;
            margin-bottom: 20px;
        }
        .top-navigation a {
            background-color: #006B3C;
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }
        .top-navigation a:hover {
            background-color: #FDB813;
            color: #000;
        }
        h1 {
            color: #006B3C;
            margin-bottom: 20px;
            text-align: center;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: left;
        }
        th {
            background-color: #006B3C;
            color: white;
        }
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
        tr:hover {
            background-color: #e2e2e2;
        }
        .no-reports {
            text-align: center;
            padding: 20px;
            color: #555;
        }
        .action-buttons button {
            background-color: #006B3C;
            color: white;
            border: none;
            padding: 8px 12px;
            border-radius: 5px;
            cursor: pointer;
            font-size: 14px;
            margin-right: 5px;
            transition: background-color 0.3s ease;
        }
        .action-buttons button:hover {
            background-color: #FDB813;
            color: #000;
        }
        /* Flash messages styling (these should be handled by base.html now, but kept for standalone fallback) */
        .flash-messages {
            list-style: none;
            padding: 0;
            margin: 20px 0;
        }
        .flash-messages li {
            padding: 10px 20px;
            margin-bottom: 10px;
            border-radius: 5px;
            color: white;
            font-weight: bold;
        }
        .flash-messages li.error { background-color: #dc3545; }
        .flash-messages li.success { background-color: #28a745; }
        .flash-messages li.info { background-color: #17a2b8; }
        .flash-messages li.warning { background-color: #ffc107; color: #333;}
    </style>
</head>
<body>
    {# This page now extends base.html to inherit its structure #}
    {% extends "base.html" %}

    {# Define the title block for this specific page #}
    {% block title %}Admin - Reported Adverts{% endblock %}

    {% block content %}
    <div class="container">
        <div class="top-navigation">
            <a href="{{ url_for('home') }}">Home</a>
            <a href="{{ url_for('profile') }}">Profile</a>
            <a href="{{ url_for('saved_adverts') }}">Saved Adverts</a>
            <a href="{{ url_for('reported_adverts_admin') }}">Admin: Reported Adverts</a>
        </div>

        <h1>Reported Adverts</h1>

        {% if reported_adverts %}
        <table>
            <thead>
                <tr>
                    <th>Report ID</th>
                    <th>Advert ID</th>
                    <th>Advert Title</th>
                    <th>Reporter ID</th>
                    <th>Reporter Username</th>
                    <th>Reason</th>
                    <th>Reported At</th>
                </tr>
            </thead>
            <tbody>
                {% for report in reported_adverts %}
                <tr>
                    <td>{{ report.report_id }}</td>
                    <td>{{ report.advert_id }}</td>
                    <td><a href="{{ url_for('reported_advert_details', report_id=report.report_id) }}" target="_blank">{{ report.advert_title }}</a></td>
                    <td>{{ report.reporter_id }}</td>
                    <td><a href="{{ url_for('profile', user_id=report.reporter_id) if report.reporter_id else '#' }}" target="_blank">{{ report.reporter_username }}</a></td>
                    <td>{{ report.reason }}</td>
                    <td>{{ report.reported_at.strftime('%Y-%m-%d %H:%M:%S') }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        {% else %}
        <p class="no-reports">No new reported adverts at this time.</p>
        {% endif %}
    </div>
    {% endblock %}
</body>
</html>
